# Справочник по MongoDB — карта книги

Продукт отдельный от «Справочника по Python»: своя палитра, свои блоки
(документ, коллекция, конвейер), свои учебные базы. Автор — Макаров М. Н.

## Принцип порядка

Каждая глава опирается только на то, что уже разобрано. Оператор не может
появиться в примере раньше своей главы. Отсюда порядок: сначала CRUD и курсор,
потом язык фильтров, потом форма и скорость, потом агрегация, и только в конце —
выражения над массивами и связи между коллекциями.

Источник материала — 13 файлов `python_reference_part2_pages_717_920`. Разделы,
которых в исходнике нет (CRUD-методы, операторы обновления, индексы, `explain`),
дописаны: без них примеры из исходника нечем выполнить.

## Учебные базы

Четыре базы, чтобы примеры не сливались в один пример. Все — на стенде курса,
восстанавливаются одной командой из репозитория практик.

| База | Коллекции | Для чего в книге |
|---|---|---|
| `shop` | `products` 21, `orders` 120, `customers` 20 | CRUD, агрегация, витрины продаж |
| `hh` | `resumes` 9, `vacancies` 8, `companies` 8, `interviews` 60 | язык фильтров, продолжение занятий 1–2 |
| `logs` | `events` 1200 | индексы, `explain`, группировка по времени |
| `org` | `employees` 15, `categories` 10 | `$graphLookup`, иерархии |

## Модули

### Модуль 1 · Подключение и первые данные — `shop`
1.1 Сервер, база, коллекция: три уровня хранения
1.2 Документ и BSON: типы значений и поле `_id`
1.3 Вставка: `insert_one` и `insert_many`
1.4 Чтение: `find`, `find_one` и курсор
1.5 Проекция: какие поля вернуть
1.6 Порядок и порции: `sort`, `limit`, `skip`, `count_documents`
1.7 Обновление: `$set`, `$inc`, `$unset`, `upsert`
1.8 Удаление и как не удалить лишнего
→ **ПР-01** «Каталог товаров: витрина и правки» · **КТ-01** «Инвентаризация склада»

### Модуль 2 · Язык фильтров — `hh`, `shop`
2.1 Фильтр — это документ. Точечная нотация
2.2 Сравнение: `$eq` `$ne` `$gt` `$gte` `$lt` `$lte`
2.3 Списки значений: `$in`, `$nin`
2.4 Логика: `$and` `$or` `$not` `$nor`
2.5 Есть ли поле: `$exists` и `$type`
2.6 Массивы: `$all`, `$elemMatch`, `$size`
2.7 Текстовые шаблоны: `$regex` и `$options`
2.8 Сравнение полей между собой: `$expr`
→ **ПР-02** «Подбор кандидатов» · **КТ-02** «Починить сломанные запросы»

### Модуль 3 · Форма данных и скорость — `shop`, `logs`
3.1 Правила на уровне базы: `$jsonSchema`
3.2 Индексы: одиночные, составные, уникальные
3.3 Как читать `explain`: COLLSCAN против IXSCAN
3.4 Текстовый поиск: текстовый индекс, `$text`, `$search`
3.5 География: `2dsphere`, `$near`, `$geoWithin`, `$geoIntersects`
→ **ПР-03** «Журнал сервиса: индексы и правила» · **КТ-03** «Замедлившийся отчёт»

### Модуль 4 · Конвейер агрегации — `shop`
4.1 Конвейер и стадия `$match`
4.2 `$project`: форма ответа
4.3 `$addFields`, `$set`, `$unset`
4.4 `$group` и аккумуляторы `$sum` `$avg` `$min` `$max`
4.5 `$sort`, `$limit`, `$skip`, `$count`, `$sortByCount`
4.6 `$unwind`: массив становится документами
4.7 `$push`, `$addToSet`, `$first`, `$last`
4.8 `$bucket`, `$bucketAuto`, `$facet`
4.9 `$replaceRoot`, `$replaceWith`, `$redact`
→ **ПР-04** «Витрина продаж за квартал» · **КТ-04** «Отчёт для руководителя»

### Модуль 5 · Выражения над массивами — `shop`, `logs`
5.1 `$arrayElemAt` и `$slice`
5.2 `$filter`
5.3 `$map`
5.4 `$reduce`
5.5 `$let`
→ **ПР-05** «Чек заказа: пересчёт позиций»

### Модуль 6 · Связи между коллекциями — `shop`, `org`, `hh`
6.1 `$lookup`: соединение коллекций
6.2 `$lookup` с конвейером
6.3 `$graphLookup`: иерархии
6.4 `$unionWith`
6.5 `$merge`: материализованная витрина
→ **ПР-06** «Оргструктура и витрина заказов» · **КТ-итог** «Аналитический контур»

## Устройство главы

Паспорт темы → содержание → механизм с визуализацией → запрос в двух видах
(оболочка и PyMongo) → кадр Compass с этим же запросом на реальных данных →
частые ошибки → проверьте себя → шпаргалка.

## Практики и контрольные точки

Практика идёт после логически завершённого набора тем: 1–2 прикладные задачи,
разобранные до кода, со ссылкой на готовое решение в репозитории
`mongodb-practice` (папка `practiceNN`: README, данные, решение, самопроверка).

Контрольная точка — самостоятельная работа на тех же данных, но без разбора:
студент получает задание уровня рабочей задачи и оформляет результат отчётом.
